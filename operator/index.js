/* 
Operators are special type of methods which take one or more then one operands as arguments and return value as result.
4+5 (9 is return value as result)
4 and 5 are operands
+ is an example of operator
*/
let x = 10;
let y = 20;
let z = x + y;
// in this example + behave on 10 and 20 rather then x and y
// return 30 which we are assign in z variable now z value is 30 
// there are 2 operator = and +. Here + have high precidency  from = so first it evaluate + then it assign( evaluate =)